# Repository guidance

- Edit skills and Amp plugins only in this repository's `ai/` directory; follow `ai/AGENTS.md` there. Installed/global copies are publication outputs, not editing locations. Publish only when explicitly authorized via the documented `dot-sync-amp` workflow.
